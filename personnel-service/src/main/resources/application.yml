server:
  port: 9091
spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: HRSystem
      username: devAvs
      password: musketeers!123
  cloud:
    config:
      enabled: false
  rabbitmq:
    host: localhost
    port: 5672
    username: user
    password: user
personnel-service-config:
  jwt:
    issuer: ${HR_SYSTEM_ISSUER}
    secret-key: ${HR_SYSTEM_KEY}
  rabbitmq:
    auth-exchange: authExchange
    personnel-register-queue: personnelRegisterQueue
    personnel-register-bindingKey: personnelRegisterBindingKey
    personnel-activation-queue: personnelActivationQueue
    personnel-activation-bindingKey: personnelActivationBindingKey






#spring:
#  cloud:
#    config:
#      uri: ${config-server-url} #http://localhost:8888
#  config:
#    import: "configserver:"
#  application:
#    name: person-service-application